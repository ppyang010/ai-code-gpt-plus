<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.aichat.modules.admin.mapper.AdminUserMapper">

    <select id="selectByUsername" resultType="com.example.aichat.modules.admin.entity.AdminUserDO">
        SELECT *
        FROM admin_user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <select id="selectActiveById" resultType="com.example.aichat.modules.admin.entity.AdminUserDO">
        SELECT *
        FROM admin_user
        WHERE id = #{adminUserId}
          AND status = 1
        LIMIT 1
    </select>

    <update id="updateLastLoginAt">
        UPDATE admin_user
        SET last_login_at = #{lastLoginAt},
            updated_at = NOW()
        WHERE id = #{adminUserId}
    </update>

</mapper>
