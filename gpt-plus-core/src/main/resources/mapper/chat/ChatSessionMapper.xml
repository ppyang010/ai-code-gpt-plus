<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.aichat.modules.chat.mapper.ChatSessionMapper">

    <select id="selectPageByUserId" resultType="com.example.aichat.modules.chat.entity.ChatSessionDO">
        SELECT *
        FROM chat_session
        WHERE user_id = #{userId}
          AND status = 1
        ORDER BY last_message_at DESC, id DESC
    </select>

    <select id="selectActiveById" resultType="com.example.aichat.modules.chat.entity.ChatSessionDO">
        SELECT *
        FROM chat_session
        WHERE id = #{sessionId}
          AND user_id = #{userId}
          AND status = 1
        LIMIT 1
    </select>

    <update id="updateLastMessageAt">
        UPDATE chat_session
        SET last_message_at = #{lastMessageAt},
            updated_at = NOW()
        WHERE id = #{sessionId}
    </update>

</mapper>
