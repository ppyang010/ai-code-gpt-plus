<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.aichat.modules.billing.mapper.UserTokenUsageMapper">

    <select id="selectDailyUsage" resultType="com.example.aichat.modules.billing.entity.UserTokenUsageDO">
        SELECT *
        FROM user_token_usage
        WHERE user_id = #{userId}
          AND stat_date = #{statDate}
        ORDER BY id ASC
    </select>

    <select id="sumTotalTokensByUserId" resultType="java.lang.Long">
        SELECT COALESCE(SUM(total_tokens), 0)
        FROM user_token_usage
        WHERE user_id = #{userId}
    </select>

    <select id="sumEstimatedCostByUserId" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(estimated_cost), 0)
        FROM user_token_usage
        WHERE user_id = #{userId}
    </select>

</mapper>
